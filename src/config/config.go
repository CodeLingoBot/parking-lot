// Package config stores global applcation configurations
package config

import (
	"ptypes"
)

const (
	// Capacity Parking default value
	Capacity = ptypes.Capacity(0)
	// Start : Slot start index in Parking default value
	Start = ptypes.Index(1)
)
